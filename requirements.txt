# 🚀 Python 3.13 Compatible Requirements
# These versions actually work with Python 3.13!

# Core web framework - v2 with pydantic v2 support
fastapi==0.104.1

# ASGI server - compatible with FastAPI 0.104.1
uvicorn[standard]==0.24.0

# Database adapter - pure Python, no binary issues
psycopg2==2.9.6

# Environment management
python-dotenv==1.0.0

# Data validation - v2 for Python 3.13 compatibility
pydantic==2.5.0
