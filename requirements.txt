# 🚀 No Pydantic Requirements (Pure Python Only!)
# These packages have NO Rust dependencies and work with Python 3.13!

# Core web framework - pure Python
fastapi==0.95.2

# ASGI server - pure Python
uvicorn[standard]==0.22.0

# Database adapter - pure Python, no binary issues
psycopg2==2.9.6

# Environment management
python-dotenv==1.0.0
